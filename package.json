{
  "name": "stoagroup-api-client",
  "version": "1.0.0",
  "description": "API client for STOA Group Database (used by Domo dashboards and other repos). Single file: api-client.js.",
  "main": "api-client.js",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix api",
    "distribute": "node scripts/copy-api-client-to-dashboards.js"
  }
}
